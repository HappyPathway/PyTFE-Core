resource "tfe_team_access" "{{ team_access.workspace }}_{{ team_access.team_id }}" {
  access = "{{ team_access.access }}"
  team_id = "{{ team_access.team_id }}"
  workspace_id = "{{ team_access.workspace_id }}"
}